[package]
name = "diesel-cte-ext"
version = "0.2.0"
edition = "2024"
description = "Common Table Expressions for Diesel"
readme = "README.md"
license = "ISC"

resolver = "2"

[dependencies]
diesel = { version = "2", default-features = false }
diesel-async = { version = "0.7", default-features = false, optional = true, features = ["postgres", "sqlite", "tokio", "async-connection-wrapper"] }

[lints.clippy]
pedantic = { level = "warn", priority = -1 }

# 1. hygiene
allow_attributes                    = "deny"
allow_attributes_without_reason     = "deny"
blanket_clippy_restriction_lints    = "deny"
cognitive_complexity                = "deny"
needless_pass_by_value              = "deny"
implicit_hasher                     = "deny"

# 2. debugging leftovers
dbg_macro                           = "deny"
print_stdout                        = "deny"
print_stderr                        = "deny"

# 2. panic-prone operations
unwrap_used                         = "deny"
expect_used                         = "deny"
indexing_slicing                    = "deny"
string_slice                        = "deny"
integer_division                    = "deny"
integer_division_remainder_used     = "deny"
panic_in_result_fn                  = "deny"
unreachable                         = "deny"

# 4. portability
host_endian_bytes                   = "deny"
little_endian_bytes                 = "deny"
big_endian_bytes                    = "deny"

# 5. nursery idiom polish
let_underscore_must_use             = "deny"
or_fun_call                         = "deny"
option_if_let_else                  = "deny"
self_named_module_files             = "deny"
shadow_reuse                        = "deny"
shadow_same                         = "deny"
shadow_unrelated                    = "deny"
str_to_string                       = "deny"
string_lit_as_bytes                 = "deny"
try_err                             = "deny"
unneeded_field_pattern              = "deny"
use_self                            = "deny"

# 6. numerical foot-guns
float_arithmetic                    = "deny"
cast_possible_truncation            = "deny"
cast_possible_wrap                  = "deny"
cast_precision_loss                 = "deny"
lossy_float_literal                 = "deny"

# 7. API ergonomics
missing_const_for_fn                = "deny"
must_use_candidate                  = "deny"
unused_async                        = "deny"

# 8. Error handling
missing_panics_doc                  = "deny"
error_impl_error                    = "deny"
result_large_err                    = "deny"

[lints.rust]
missing_docs                        = "deny"

[lints.rustdoc]
missing_crate_level_docs            = "deny"

[features]
default = ["sqlite", "postgres"]
sqlite = ["diesel/sqlite", "diesel-async/sqlite"]
postgres = ["diesel/postgres", "diesel-async/postgres"]
async = ["dep:diesel-async"]

[dev-dependencies]
diesel = { version = "2", default-features = false, features = ["sqlite", "postgres", "chrono"] }
diesel-async = { version = "0.8", features = ["sqlite", "postgres", "tokio", "async-connection-wrapper"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
rstest = "0.26"
pg-embed-setup-unpriv = { version = "0.5.0", features = ["diesel-support"] }
serde_json = "1"
toml = "0.9"
